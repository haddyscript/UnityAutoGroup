import { PagePlaceholder } from '../components/shared/PagePlaceholder'

export default function Contact() {
  return (
    <PagePlaceholder
      title="Contact"
      description="Contact details and form coming in Phase 3, once the client confirms the preferred format."
    />
  )
}
